eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: "http://localhost:8761/eureka"
spring:
  application:
    name: mainserver
server:
  port: 8761
